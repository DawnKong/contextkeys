<Window x:Class="ContextKeys.Services.ToastWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="" Width="260" MaxHeight="120"
        Background="Transparent"
        WindowStyle="None"
        AllowsTransparency="True"
        ShowInTaskbar="False"
        Topmost="True"
        ResizeMode="NoResize"
        IsHitTestVisible="False"
        Focusable="False"
        WindowStartupLocation="Manual">
    <Border Background="Transparent" Padding="0">
        <Border Style="{StaticResource CardStyle}"
                BorderBrush="{StaticResource KeycapBorderBrush}"
                Background="{StaticResource MainSurfaceBrush}"
                CornerRadius="10"
                Padding="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="0,0,0,4">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="3" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <Rectangle Grid.Column="0" Fill="{StaticResource PrimaryBrush}"
                               Width="3" RadiusX="1.5" RadiusY="1.5"
                               HorizontalAlignment="Left" VerticalAlignment="Stretch" />

                    <TextBlock Grid.Column="1" x:Name="ToastTitle" FontSize="12" FontWeight="SemiBold"
                               Foreground="{StaticResource TextPrimaryBrush}" Margin="6,0,0,0">
                        已启用：ChatGPT 快捷语
                    </TextBlock>
                </Grid>

                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" 
                             HorizontalScrollBarVisibility="Hidden" Margin="0" Padding="0" MaxHeight="70">
                    <ItemsControl x:Name="ToastRules">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="0,1,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <Border Grid.Column="0" Style="{StaticResource KeycapStyle}"
                                            Height="16" Padding="3,0" Margin="0,0,4,0">
                                        <TextBlock Style="{StaticResource KeycapTextStyle}" FontSize="10">
                                            <Run Text="{Binding Hotkey.Display}" />
                                        </TextBlock>
                                    </Border>
                                    <TextBlock Grid.Column="1" FontSize="11"
                                               Foreground="{StaticResource TextSecondaryBrush}"
                                               VerticalAlignment="Center"
                                               Text="{Binding Name}" />
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>
        </Border>
    </Border>
</Window>
